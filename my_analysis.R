# write some code 
my_vector <- 1:100
mean(my_vector)
hist(my_vector)

#type soem stuff 
median(my_vector)

# Need to always make sure to save the script, or else it wont work with git 

# Tick the boxes (Add turns green)
# Git commit step by pressing the "Commit" button which 
# Good commit messages are normally descriptive 